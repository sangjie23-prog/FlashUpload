server:
  port: 8080

spring:
  application:
    name: flash-upload-backend
  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 100MB

flash-upload:
  storage:
    path: storage/files

# 第二阶段先使用本地磁盘保存普通上传文件，后续再扩展数据库和分片配置。
